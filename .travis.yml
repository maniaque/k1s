dist: xenial
language: generic
addons:
  apt:
    packages:
      - iproute2
install: sudo ./install
script:
  - IFACE=$(ip r | grep '^default' | grep -o 'dev .* proto' | cut -d' '  -f2)
  - echo $IFACE
# deploy: sudo ./init 